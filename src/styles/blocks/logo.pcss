/* Logo
============================================================================================ */
.logo {
  color: $links-color;
  font-size: 36px;
  font-weight: 800;
  line-height: 30px;
  text-transform: uppercase;
  transition: color .5s ease-in;
}

.overlay {
  .logo {
    color: #fff;
  }
}