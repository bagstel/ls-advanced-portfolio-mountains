/* Burger
============================================================================================ */
.burger {
  display: flex;
  align-self: center;
  justify-self: center;
  &__icon {
    width: 24px;
    height: 20px;
    fill: $links-color;
  }
}